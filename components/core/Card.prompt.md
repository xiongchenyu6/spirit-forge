Surface plate (炼器台). The default container for grouped content.

```jsx
<Card>普通卡片内容</Card>
<Card cut glow="gold" interactive>切角发光卡片</Card>
```

`cut` enables the 切角 corner motif; `glow`: none | gold | jade; `interactive` adds hover lift.
