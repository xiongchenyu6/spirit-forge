Single-line text field.

```jsx
<Input placeholder="搜索素材…" icon={<i data-lucide="search" />} />
<Input defaultValue="64" suffix="px" size="sm" />
```

Focus shows a gold ring. `invalid` switches to cinnabar.
