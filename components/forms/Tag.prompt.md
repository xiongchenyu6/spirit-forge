Selectable pill/chip — the workhorse of the 创作台 parameter panel (题材/风格/视角/动作/尺寸/用途/格式).

```jsx
<Tag selected>仙侠</Tag>
<Tag>赛博朋克</Tag>
<Tag icon={<i data-lucide="box" />}>像素风</Tag>
```

Selected state fills gold-tinted with a glow. Group several in a flex-wrap row.
