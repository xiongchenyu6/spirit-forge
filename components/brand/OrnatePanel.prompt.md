Gold-rimmed plate frame (9-slice) over a surface color. Set `window.LF_ICON_BASE`.

```jsx
<OrnatePanel glow><h3>炼器台</h3><p>面板内容…</p></OrnatePanel>
```

`surface` sets the inner fill; `slice` tunes the frame band width.
