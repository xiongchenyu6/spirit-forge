# Asset Library · 我的素材库

`index.html` — top bar + left collections/projects nav + main asset grid. Each asset card: per-genre themed thumbnail (checkerboard transparency), name, type, format badges (PNG/GIF/Sprite/JSON), size. 题材 filter chips + search + grid/list toggle. Composes `Button`, `IconButton`, `Input`, `Badge`, `Tag`, `NavItem`, `Avatar`, `SpiritStone`.
